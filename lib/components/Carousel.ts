import { css, html, LitElement } from "lit";
import { customElement } from "lit/decorators.js";

@customElement("se-carousel")
export class Carousel extends LitElement {
  static styles = css``;

  protected render() {
    return html`Carousel works!`;
  }
}
